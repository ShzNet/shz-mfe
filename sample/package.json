{
  "name": "shz-mfe-sample",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "nx run-many -t dev --parallel=10",
    "dev:host-main": "nx dev host-main",
    "dev:remote-crm": "nx dev remote-crm",
    "dev:remote-hr": "nx dev remote-hr"
  },
  "devDependencies": {
    "@shz/nx-generators": "0.0.6-local.7",
    "nx": "^21.0.0"
  }
}
