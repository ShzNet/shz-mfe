{
  "name": "nx-generators",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "root": "tools/nx-generators",
  "sourceRoot": "tools/nx-generators",
  "tags": ["scope:tooling", "type:plugin", "publishable"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "pnpm build",
        "cwd": "{projectRoot}"
      }
    },
    "nx-release-publish": {
      "executor": "@nx/js:release-publish",
      "dependsOn": ["build"],
      "options": {
        "packageRoot": "{projectRoot}/dist",
        "registry": "https://registry.npmjs.org"
      }
    }
  }
}
